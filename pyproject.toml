[project]
name = "data_aggregator"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "pydantic"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
    "mypy",
]

